print #prints whats written after
# #comment
+ #add
- #substract
* #multiply
/ #devide
% #modulos
+= #plus itself
< #less than
> #greater than
<= #lesser than or equal
>= #greater than or equal
!= #not equal
== #equals
= #assigns value to variable
varible #a storage location assigned with identification name
integer #whole digit number
float #numbers with decimals
string #sequence of characters
boolean #a variable with two possible values (true, false)
concatenation #joining several strings together
formatters #formates strings with within variables
%s #string
%d #digit
%r #raw data string
'\ '#escapes certain characters from the script
'\n' #new line
'\t' #tab
“““ #string across several lines
raw_input() #prompts user to input
int(raw_input()) #converts user input to integer
import #import other python modules into script
modules #features
argv #list of command line arguments
open() #opens external file inside script
read() #read open file
close() #close file
write() #writes srting to file
truncate() #clears file
seek() #sets the file‘s current position at the offset
readline() #reads one line of file
len() #returns the length of an object
functions #named section of script which performs certain task
def #defining new functions
arguments #input parameters
return #returns value from function
exists #checks existance of files
pydoc #python documentation
